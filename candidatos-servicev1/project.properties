# Identificador único del proyecto
sonar.projectKey=candidatos-servicev1

# Nombre del proyecto (como aparecerá en SonarQube)
sonar.projectName=candidatos-servicev1

# Versión del proyecto
sonar.projectVersion=1.0

# Directorio donde se encuentra el código fuente
sonar.sources=src

# Directorio donde se encuentra el código fuente
sonar.sources=src

# Directorio donde se ubican las clases compiladas (si usas Maven, apunta a target/classes)
sonar.java.binaries=target/classes

# (Opcional) Excluir tests o carpetas que no quieras analizar
# sonar.exclusions=**/test/**